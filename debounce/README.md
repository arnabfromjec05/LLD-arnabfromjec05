Debounce/throttling usage comes for rate-limiting the number of API calls made.

- search engines (to limit autosuggest calls which would unless made on every keystroke of user)
- scroll tracking
- page resizing
